{
  "spec_commit": "b39c3276821a6dc3dd4a0f7e1f63c48cb15e269b",
  "dart_sass": "1.104.1",
  "attempted": 14258,
  "pass": 11559,
  "error_expected": 2492,
  "passing": 14051,
  "note": "The COMPRESSED-style ratchet. sass-spec ships dart-sass's expanded output as a case's only expectation, so `pass` here is measured against spec/COMPRESSED_EXPECT.txt instead: per-case digests of the compressed CSS dart-sass 1.104.1 emits for the same cases (spec/gen_compressed.py). error_expected is style-independent and matches spec/BASELINE.json exactly. The gap to that file's pass count -- 11559 vs 11622 -- is 63 cases whose expanded CSS is byte-exact and whose compressed CSS is not. 62 of them are cases where dart-sass 1.104.1's own expanded output no longer matches the expectation sass-spec ships, so they cannot pass both ratchets at once; the 63rd is the one real compressed-serialization divergence left: a generic at-rule reached through name interpolation (css/unknown_directive/name_interpolation), where dart keeps the node generic and we re-read the name it spells. Raise this number, never lower it; bump this file in the same commit as the fix."
}
